<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.utsnabiladatatif">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Utsnabiladatatif"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Tambahkan activity homepage -->
        <activity
            android:name=".homepage"
            android:exported="true" />

        <!-- Tambahkan activity frompage -->
        <activity
            android:name=".frompage"
            android:exported="true" />

        <!-- Tambahkan activity Diterimal -->
        <activity
            android:name=".Diterimal"
            android:exported="true" />
        <activity
            android:name=".ditolak"
            android:exported="true" />
        <activity
            android:name=".menunggu"
            android:exported="true" />


    </application>

</manifest>
